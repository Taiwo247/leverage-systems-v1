---
---

<nav class="fixed top-0 left-0 right-0 z-50 flex items-center justify-between px-6 md:px-12 py-5 border-b border-white/5 backdrop-blur-md bg-[#0A0A0A]/80">
  <!-- Logo -->
  <a href="/" class="flex items-center gap-3 group">
    <div class="w-8 h-8 bg-[#00FF41] flex items-center justify-center rounded-sm">
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
        <path d="M2 14L8 2L14 14" stroke="#000" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
        <path d="M4.5 9.5H11.5" stroke="#000" stroke-width="2.5" stroke-linecap="round"/>
      </svg>
    </div>
    <span class="font-black text-white tracking-tight text-lg">LEVERAGE<span class="text-[#00FF41]">.</span></span>
  </a>

  <!-- Center links (desktop) -->
  <div class="hidden md:flex items-center gap-8 text-sm text-white/50 font-medium">
    <a href="#comparison" class="hover:text-white transition-colors">How It Works</a>
    <a href="#engine" class="hover:text-white transition-colors">Features</a>
    <a href="#vsl" class="hover:text-white transition-colors">Case Studies</a>
  </div>

  <!-- CTA -->
  <a
    href="#diagnostic"
    class="neon-btn hidden md:inline-flex items-center gap-2 px-5 py-2.5 text-sm font-bold rounded-sm"
  >
    Run Revenue Audit
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
      <path d="M5 12h14M12 5l7 7-7 7"/>
    </svg>
  </a>

  <!-- Mobile menu toggle -->
  <button id="mobile-menu-toggle" class="md:hidden flex flex-col gap-1.5 p-2" aria-label="Menu">
    <span class="w-5 h-0.5 bg-white block transition-all"></span>
    <span class="w-5 h-0.5 bg-white block transition-all"></span>
    <span class="w-3 h-0.5 bg-white block transition-all"></span>
  </button>
</nav>

<!-- Mobile menu -->
<div id="mobile-menu" class="fixed inset-0 z-40 bg-[#0A0A0A]/95 backdrop-blur-xl flex flex-col items-center justify-center gap-8 translate-y-full transition-transform duration-300 md:hidden">
  <a href="#comparison" class="text-3xl font-bold text-white" onclick="document.getElementById('mobile-menu').classList.add('translate-y-full')">How It Works</a>
  <a href="#engine" class="text-3xl font-bold text-white" onclick="document.getElementById('mobile-menu').classList.add('translate-y-full')">Features</a>
  <a href="#vsl" class="text-3xl font-bold text-white" onclick="document.getElementById('mobile-menu').classList.add('translate-y-full')">Case Studies</a>
  <a href="#diagnostic" class="neon-btn mt-4 px-8 py-4 text-lg font-bold rounded-sm" onclick="document.getElementById('mobile-menu').classList.add('translate-y-full')">Run Revenue Audit</a>
</div>

<script>
  const toggle = document.getElementById('mobile-menu-toggle');
  const menu = document.getElementById('mobile-menu');
  toggle?.addEventListener('click', () => {
    menu?.classList.toggle('translate-y-full');
  });
</script>
